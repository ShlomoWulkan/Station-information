{
  "name": "midaa-tachana-server",
  "version": "1.0.0",
  "description": "Python proxy server for Israeli Ministry of Transport SIRI API",
  "scripts": {
    "setup": "python -m venv venv && venv\\Scripts\\pip install -r requirements.txt",
    "start": "venv\\Scripts\\python app.py",
    "dev": "venv\\Scripts\\python app.py",
    "setup:unix": "python3 -m venv venv && venv/bin/pip install -r requirements.txt",
    "start:unix": "venv/bin/python app.py"
  },
  "engines": {
    "python": ">=3.11"
  }
}
